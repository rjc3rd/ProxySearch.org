# Runtime stats/log data, not source — same folder deploy-proxysearch.org already excludes.
stats/

.DS_Store

# Stamped fresh by deploy-proxysearch.org on every deploy; not source.
.deploy-version
.well-known/security.txt

# No .md files ever go to GitHub — standing rule.
*.md
